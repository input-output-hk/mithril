---
title: Distribution `2617` is now available
authors:
  - name: Mithril Team
tags: [release, distribution, 2617]
---

### Distribution `2617` is now available

The [`2617.0`](https://github.com/input-output-hk/mithril/releases/tag/2617.0) distribution has been released, introducing the following changes:

:::info

⚠️ This distribution will reset the `cardano-transaction` database of the signer and the aggregator, and trigger a new full scan of the Cardano chain.

:::

- **Mithril signer stable release**: bumped the Mithril signer to version `1.0.0`, marking it as **production-ready** on the `release-mainnet` network
- **Cardano node support**: added support for Cardano node `10.6.2` and `10.7.1` in the signer, aggregator, and client
- **Statically built binaries**: the aggregator, signer, client, and relay are now **statically built** using the `musl` target on both GitHub and Hydra CI pipelines (`glibc` is no longer required)
- **Release process**: upgraded the release process to support synchronization with Cardano node releases
- **Removed**: the `v1` backend of Cardano database is removed from the client CLI and library
- **Preliminary support for Cardano blocks and transactions certification**: storage of Cardano blocks in the signer and aggregator `cardano_tx` SQLite database
- General bug fixes and performance improvements.

This new distribution has been deployed to the **Mithril aggregator** on the `release-mainnet` and `release-preprod` networks.

For those running a **Mithril signer**:

- **pre-release-preview** network: no action is required at this time
- **release-preprod** network: upgrade the signer node binary to version `1.0.0` (no configuration changes are required)
- **release-mainnet** network: upgrade the signer node binary to version `1.0.0` (no configuration changes are required).

You can update the Mithril signer using the one-line command below. By default, it downloads to the current directory, but you can specify a custom folder with the -p option:

```bash
curl --proto '=https' --tlsv1.2 -sSf https://raw.githubusercontent.com/input-output-hk/mithril/refs/heads/main/mithril-install.sh | sh -s -- -c mithril-signer -d 2617.0 -p $(pwd)
```

For any inquiries or assistance, contact the team on the [Discord channel](https://discord.gg/5kaErDKDRq).
