# Mithril-cardano-node-internal-database

This crate provides components to read and hash the files of a Cardano node internal database, such as immutable files
and ledger state snapshots.
It includes observers, digesters and various helpers for file system operations.