use crate::MithrilStakeDistribution;
use mithril_common::entities::ProtocolMessage;

pub struct Message;

impl Message {
    pub fn compute_mithril_stake_distribution_message(
        mithril_stake_distribution: &MithrilStakeDistribution,
    ) -> ProtocolMessage {
        todo!()
    }
}
