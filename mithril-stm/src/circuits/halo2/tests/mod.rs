//! Test-only modules for the Halo2 circuit integration.
//!
//! `golden` covers end-to-end circuit scenarios, while `unit` keeps narrowly focused checks for
//! extracted gadgets and adapters.

pub(crate) mod golden;
pub(crate) mod unit;
